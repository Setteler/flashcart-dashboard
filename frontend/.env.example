# Backend API base URL — leave empty for local dev (Vite proxy handles it)
# Set this to your deployed backend URL in production, e.g.:
# VITE_API_BASE_URL=https://flashcart-backend.onrender.com
VITE_API_BASE_URL=
